# Skywatch Coordinator 项目说明

## 项目目标

Skywatch Coordinator 为野外天文观测团队协调一次夜间观测活动。协调员创建观测活动和观测站，站点操作员开启时段并提交观测帧，审核员在所有帧完成审核后发布活动结果。系统使用内存保存数据，适合离线演练与本地服务运行。

## 用户角色

- 协调员：创建活动、配置站点并查看活动概览。
- 站点操作员：开启观测时段、提交观测帧并关闭时段。
- 审核员：审核候选帧并在活动满足发布规则时发布结果。

## 核心实体

- 观测活动 Campaign：定义名称、目标天区、活动时间窗口和发布状态。
- 观测站 Station：隶属于活动，声明站点代码与海拔。
- 观测时段 Shift：绑定一个站点，记录开始、结束和当前状态。
- 观测帧 Observation：由时段提交，带有质量分数和审核状态。
- 发布快照 Release：记录一次满足完整性规则后的发布摘要。

## 业务流程

### 建立观测活动

协调员通过 `POST /v1/campaigns` 提交活动名称、目标天区与时间窗口。系统校验时间窗口和名称，创建活动后可继续添加站点；失败时返回输入错误。调用 `GET /v1/campaigns/{id}/summary` 可以验证活动已处于 active 状态。

### 执行站点观测时段

站点操作员通过 `POST /v1/shifts` 开启与活动、站点关联的时段，再通过 `POST /v1/observations` 提交观测帧。系统要求帧时间位于时段内且质量分数有效；关闭时段使用 `POST /v1/shifts/{id}/close`。查询 `GET /v1/shifts/{id}` 可以验证关闭或开启状态。

### 审核并发布观测结果

审核员使用 `POST /v1/observations/{id}/review` 对候选帧给出 accepted 或 rejected 结论。所有关闭时段中的候选帧都被审核且至少有一帧 accepted 时，使用 `POST /v1/campaigns/{id}/release` 创建发布快照；不满足完整性规则时返回失败。活动概览会显示 released 状态及已接受帧数量。

## 状态与规则

活动状态为 active、released 或 archived；发布后的活动不能再创建时段。时段状态为 open 或 closed；关闭时段不能继续接收帧。帧状态为 candidate、accepted 或 rejected。活动发布要求全部时段关闭、每个候选帧已经审核，且至少存在一个 accepted 帧。

## 接口与验证

服务提供 `/health` 健康检查及上述 JSON API。公开测试覆盖创建活动、时段与帧、审核发布、时间窗口边界、重复站点以及关闭时段后的写入限制。运行验证清单通过真实 HTTP 请求检查三个工作流。
